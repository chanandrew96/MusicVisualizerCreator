services:
  - type: web
    name: music-visualizer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: starter
    healthCheckPath: /

